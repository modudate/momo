{
  "name": "moso-homepage",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix demo",
    "build": "npm run build --prefix demo",
    "start": "npm run start --prefix demo",
    "install:all": "npm install --prefix demo"
  }
}
